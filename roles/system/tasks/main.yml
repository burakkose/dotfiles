---
- name: Upgrade the system
  pacman:
    update_cache: yes
    upgrade: yes

- name: Install packages
  command: trizen -S "{{ item }}" --noconfirm --needed
  become: no
  with_items:
      "{{ packages }}"
