#!/usr/bin/env bash
set -euo pipefail

if command -v swaymsg >/dev/null 2>&1; then
  swaymsg fullscreen disable >/dev/null 2>&1 || true
fi

if [ "$#" -eq 0 ]; then
  echo "usage: $0 <command> [args...]" >&2
  exit 1
fi

exec "$@"
